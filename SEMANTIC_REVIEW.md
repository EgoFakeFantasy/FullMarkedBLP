# 精简中的语义保持审查

此文记录本轮精简的语义审查和保留理由，逐轮验收见 REFACTORING.md。
优先次序为证明有效性、数学含义、人类可审查性、减行。

## 固定的数学对象

不改写原始五行根、零项、行型、复制筛选、cut、E、native、完整 M_star、
Step、Generated、Reachable、短键或 RankI2。最终定理仍针对全部原始生成的
字面项，不添加语义过滤；短键单射性仍辨认包含 marks 的整个字面模式。

局部对照从原有编译环境导出 1,796 个源码声明的完整类型和宇宙参数，
并保存其中非定理声明的定义体。还对照 320 个构造器、字段投影、递归器
及实例，共 2,116 个对象；结构的字段也在检查范围。有意改名的五个存在
定理使用明确的一一映射。比较范围包括显式及隐式参数，避免把前提变化、
量词顺序变化或定义改写当作证明压缩。编译器辅助定理另由 Audit.lean 遍历
编译环境、收集传递公理依赖，不用文本搜索代替内核审计。

## 公共证明的具体代换

| 改动 | 删除部分由什么替代 | 保留的数学边界 |
|---|---|---|
| 三种补全事件的行实现 | completionEvent_realization 统一组装核心、临界点和标记证书；三个调用点继续各自证明边与证书 | 直接入口、中间直接事件、复制后的任意词事件仍有各自接口 |
| native 块的重复几何计算 | 从普通行型、实际源计算和删除 owner 的定义推出四个公共事实 | marks、trace、端点、临界点及证书的归纳仍分别可读 |
| 三个端点上界 | 对现有精确值等式使用 ≤；历史记录沿用精确值定理构造的同一组见证 | 不新增来源、历史有效性或事件几何前提 |
| 带 Sat 的 trace/cut | 先取原有行实现分量，使用普通行实现定理；cut 另调用原有 Sat 保持性 | 两种结构的字段及所有原有定理陈述不变 |
| 存在定理命名 | 根族与 I2 根实现采用 exists_ 前缀，整体迁移调用点与文档 | 最终主定理名称及其类型保持稳定，不增加旧名别名 |

CompletionEventGeometry 和证书参数只是公共引理的局部输入；三个原有调用点
仍构造这些输入。它们没有变成最终主定理的新假设。类似地，行实现到带 Sat
实现的组装使用已经给出的 h.sat，不把所需对象的存在性改成一个未构造的结构。

## 保留的接口及说明

初始静态分析列出 37 个不在最终定理导入闭包内的模块，以及 155 个没有其他
编译常量调用的源码定理。它们是审查线索，不能据此认定为死代码。保留其
数学声明，包括模块整理后移动到同名行实现文件中的结果。

- 原始 cut、shortCopy、native 与 fullScan 的成功性、保持性及具体根例子，
  帮助读者确认采用的是稿件的实际算法。
- 完整共同端点有限 lining 族、真实秩实现和其保持性，明确区别于单个或
  任意挑选的有限见证。
- 自然截断、trace 的唯一性与传输、记录诞生位置及精确端点值，说明局部
  证书为何适用于实际扫描。
- 一般反射、有限可表达性、固定参数反射和真实 Steel 良基接口，展示 I2
  起点与展开良基的不同数学职责。
- 可达性比较、短键严格下降、键相等刻画、单射性和最终良序，保留整条
  比较桥的可检查里程碑。

不把 native 的不同性质归纳合成携带大量参数的高阶框架；这些证明涉及
不同的 ambient pattern、索引和证书，保留各自的归纳有利于人工审查。
也不为了省少量参数行，把小模块的假设藏入大范围 section，或压缩有限
公式编码中具有不同变量位置的证明分支。

## 验收和范围

每个正式工作区步骤以前一绿色提交为回滚点，完整构建、最终原始类型检查、
公理审计和声明对照全部成功后才提交。失败立即撤回该步；候选文件和日志
放在仓库外，不计入发布源码或减行结果。最后重新检查全部来源引用、导入
闭包、公开接口及统计，才同步 GitHub。

原有形式化边界不变：RankI2 采用非平凡 Σ₂ 初等秩嵌入表述，没有新增与
全局类嵌入表述的等价性证明，也没有新增原型 g_(11) 的精确数值估计。
这是合理冗余的精简验收，不声称代码达到数学意义上的最短形式。
