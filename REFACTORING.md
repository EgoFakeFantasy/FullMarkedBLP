# 证明精简记录

## 原始基线

- 提交：`b006cc075da934bb0cd8506d990f781c10ba6a8a`。
- 统计范围：版本控制中的全部 `.lean` 文件，排除 `.lake` 依赖与未发布探针。
- 514 个文件；33,501 行；30,205 非空行；LF 归一化 UTF-8 内容 1,729,193 字节。
- 默认构建 1500 项通过；编译环境审计 4165 个定理常量通过。
- 最终定理接受的前提为 `RankI2`，生成域、原始操作与短键保持不变。

## 实施与验收约定

1. 固定基线和最终类型合同；每个独立改动以前一个已通过的本地提交为回滚点。
2. 精简重复导入，统一集中公理审计；保持完整公开环境。
3. 按编译依赖和源码引用逐项处理冗余包装与被替代分支，保留有解释价值的语义接口。
4. 检查全库重复结构，以有数学含义的小引理合并必要重复。
5. 按 ProofNaming.md 迁移完整接口、更新调用点与文档；保持可读性。
6. 全部完成后重新构建、核对最终定理原始类型、审计全部剩余定理，才同步远程。

每个较大步骤均运行默认构建、`CheckMainTheorem.lean` 与 `Audit.lean`。
一旦任一步失败，立即恢复该步开始时的源码，保留外部失败日志，重新寻找可行方案。
不以缩小生成域、加强 I2 前提、增加语义过滤或新增公理完成精简。
行数报告同时记录物理行与非空行，并给出本步和累计净节约。

补充采用用户提供的 PCF 证明保持精简记录（2026-09-10）：优先次序为
证明有效性、数学含义、人类可审查性、减行。未调用的公开刻画和数学里程碑
不按引用次数删除；普通存在结论不能改成假设已存在的结构。每轮除了构建和
公理检查，还对照原有源码声明的完整类型、宇宙参数以及非定理定义体。
候选写法先放在仓库外检查；正式工作区每轮从干净的已验证提交开始。
用户要求的“失败即回滚”优先于参考记录中允许就地诊断修正的宽松版本。

## 已接受步骤

| 步骤 | 总行数 | 本步节约 | 累计节约 | 非空行 | 验证 |
|---|---:|---:|---:|---:|---|
| 基线 | 33,501 | 0 | 0 | 30,205 | 构建、最终类型、审计 |
| 入口导入与集中审计 | 32,958 | 543 | 543 | 29,729 | 1500 项；4165 定理；最终类型通过 |
| 补全实现公共证明 | 32,874 | 84 | 627 | 29,645 | 1501 项；4164 定理；类型对照通过 |
| 端点估计复用精确值 | 32,853 | 21 | 648 | 29,624 | 1501 项；4161 定理；类型对照通过 |
| native 块公共几何事实 | 32,692 | 161 | 809 | 29,459 | 1501 项；4099 定理；类型对照通过 |

第一轮把入口的 443 条导入收敛为 30 条，传递闭包仍包含全部 511 个模块；
删除 66 处零散公理打印，保留集中内核审计。非空行净减 476 行，另清理了
随打印命令遗留的空行；没有删除数学声明。当前 LF 归一化 UTF-8 内容为
1,710,122 字节，累计节约 19,071 字节。

补全实现公共证明：将三处相同的行实现组装过程合并为 completionEvent_realization；入口直接事件、中间直接事件及复制后任意词事件的原有定理接口均保留。 当前 515 个 Lean 文件，1,706,122 字节；全部 1796 个原有源码声明的类型、宇宙参数和定义体对照通过。

端点估计复用精确值：将 native 底部、整个扫描步和历史记录的三个上界证明改为现有精确端点定理的推论；全部原有估计接口及其前提保持不变。 当前 515 个 Lean 文件，1,704,948 字节；全部 1796 个原有源码声明的类型、宇宙参数和定义体对照通过。

native 块公共几何事实：复用非空源步数、连续目标范围、短行判定和 medium 核心合法性四个引理；18 个模块保留原有性质归纳。另加入 320 个构造器、字段投影、递归器和实例的对照，总计 2116 个对象无差异。 当前 515 个 Lean 文件，1,699,841 字节；全部 1796 个原有源码声明的类型、宇宙参数和定义体对照通过。

## 失败与回滚

目前没有修改正式证明后触发的失败。
